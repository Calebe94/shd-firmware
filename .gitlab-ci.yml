stages:
    - build
variables:
    GIT_SUBMODULE_STRATEGY: recursive

default:
    image: shaguarger/platformio:5.1.1

build-firmware:
    stage: build
    script:
        - pio run
        - ls -al .pio/
    only:
        - main
